const myModule = {
  init: function init() {
    
  },
  
  someBehaviour: function animate() {
    
  },
};

const createInstance = function createInstance() {
  return Object.create(myModule);
};

export default createInstance;
